<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:color="@color/teal_primary" android:state_focused="true" />
    <!-- Default (unfocused) state - use a visible color for Dark Mode -->
    <item android:alpha="0.6" android:color="@color/teal_primary" />
</selector>